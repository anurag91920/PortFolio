// const { Schema } = require("mongoose");

// const HoldingsSchema = new Schema({
//     name: String,   // Name of the holding (e.g., stock symbol)
//     qty: Number,    // Quantity held
//     avg: Number,    // Average buy price
//     price: Number,  // Current market price
//     net: String,    // Net profit/loss (you might consider using Number instead)
//     day: String,    // Day gain/loss (again, consider Number if it's numeric)
// });

// module.exports = { HoldingsSchema };
